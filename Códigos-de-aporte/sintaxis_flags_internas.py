def ejemplo (x) -> tuple[int, bool]:
    x = "Es un ejemplo"
    s = 0 #Es el score
    flagT : tuple = []
    flagT.append(True)
    flagT.append(False) 
    if all.flagT() == True:
        flag = True 
    else:
        flag = False 
    return s, flag
